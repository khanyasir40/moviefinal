{
  "name": "movie-app-fullstack",
  "version": "1.0.0",
  "description": "Full-stack Movie Recommendation App",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "cd backend && npm run dev",
    "install-backend": "cd backend && npm install",
    "install-frontend": "cd frontend && npm install",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": ["movie", "recommendation", "fullstack", "react", "nodejs"],
  "author": "khanyasir40",
  "license": "MIT"
}