[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['cd backend && npm install']

[phases.build]
cmds = ['echo "Backend ready for deployment"']

[start]
cmd = 'cd backend && node server.js'

[variables]
NODE_ENV = 'production'